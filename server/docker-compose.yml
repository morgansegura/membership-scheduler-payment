version: '3.9'

services:
  db:
    image: mysql:8.0.23
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    ports:
      - 3306:3306
    environment:
      MYSQL_DATABASE: nestjs
      MYSQL_ROOT_PASSWORD: root
      MYSQL_ROOT_HOST: '%'
# docker run ---name postgres-nest -p 5432:5432 -e POSTGRES_PASSWORD=S3GuRa5361980!! -d postgres
# docker container stop postgres-nest
